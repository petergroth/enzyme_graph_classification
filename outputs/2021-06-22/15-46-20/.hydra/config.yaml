model:
  conv_channels: 64
  fc_size: 128
  global_pooling: global_add_pool
  activation: nn.RReLU
  dropout: 0.1341
train:
  misc:
    lr: 0.0007514
    wandb_project: enzymes-hydra
    wandb_entity: mlops_enzyme_graph_classification
    model_path: /models/model.pth
    seed: 42
    azure: false
  trainer:
    max_epochs: 300
dataset:
  data_dir: /data/
  batch_size: 8
  num_workers: 0
  splits:
  - 0.7
  - 0.15
  - 0.15
  seed: 42
